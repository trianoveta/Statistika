library(RMySQL)
con = dbConnect(MySQL(), user = 'root', password = '', dbname = 'ps2trianoveta', host = 'localhost')
dbListTables(con)
myQuery <- "select * from vet;"
df <- dbGetQuery(con, myQuery)

library(dplyr)
df<-filter(df,Gender=="P")
print(df)
